# Copy this file to a local environment file and fill in real values.
# Never commit real secrets — local environment files are gitignored.

# OpenAI API key (used by AI-powered features such as similar-projects suggestions)
OPENAI_API_KEY=

# Upstash Redis / Vercel KV credentials (used for caching npm package data)
PP_KV_REST_API_URL=
PP_KV_REST_API_TOKEN=
PP_KV_REST_API_READ_ONLY_TOKEN=
PP_KV_URL=
PP_REDIS_URL=
